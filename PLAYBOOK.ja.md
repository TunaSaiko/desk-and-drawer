[English](PLAYBOOK.md) ｜ [简体中文](PLAYBOOK.zh.md) ｜ **日本語**

# プレイブック：AI 協働プロジェクトのドキュメント肥大化ガバナンス

> 実プロジェクトに fork した後は、そのプロジェクト内のルール文書が正です。本ファイルは上流テンプレート。
> セクション番号は三言語で対応しています（本書の §1 ＝ 中国語版の §一 ＝ 英語版の §1）。

## 1. 問題と根本原因

長期の AI 協働プロジェクトでは、「正」となるドキュメント——進捗ボード、意思決定台帳——は、**「書き方のルール」だけあって「アーカイブのルール」がない限り、必ず際限なく伸びます**。誰かが怠けているのではなく、構造の問題です：追記専用（append-only）でローテーションがなければ、増加に上限はありません。

典型的な末路（実数は README 参照）：ボードは数百 KB、一つの行は歴代エージェントの「行内追記」で数万バイトに。帰結は三連鎖です：ファイルを読むだけでコンテキストを圧迫 → 情報を読み切れない → 情報を読み間違える。**引き継ぐたびに、新しいエージェントが同じ税金を払います。**

## 2. 目標アーキテクチャ：机と引き出しの分業

| 層 | 置くもの | サイズ方針 |
|---|---|---|
| **入口ドキュメント**（HANDOFF / CLAUDE.md 系） | 「引き継ぎ方」だけ。動的状態はゼロ | 小さく安定（参考：OpenAI Codex の 32KiB ハード上限） |
| **現在の真実**（PROGRESS 系） | いまのステージ／進行中タスク／負債／候補リスト | バイト・バジェット制。超えたらローテーション |
| **台帳の live 部分**（DECISIONS 系） | 直近 30 件ほどの決定 | バイト・バジェット制 |
| **インデックス**（derived） | 一行一件の機械生成サマリ | いつでも再生成可能。**正には決してならない** |
| **アーカイブ** `docs/archive/` | あらゆる履歴ログ（追記専用） | 上限なし——コールドストレージ、必要時に grep |

読み方の規律もアーキテクチャとセットです：**履歴は必要なときに grep、通読はしない**。エージェントが大きなドキュメントを読むときは、まず見出しのアウトラインを取り、そのうえで狙った節だけを読む。

## 3. 五つのルール（すべて必経アクションに結び付ける。「思い出したら」に頼らない）

1. **ステータス行は行ごと置換。行内追記は禁止。** 置き換えられた旧行は、その場でアーカイブファイルへ原文のまま追記する。一行モンスターは「行内追記の習慣」が製造するものです。
2. **終わったらアーカイブ**：ステージが完了したらその節を丸ごとアーカイブへ移し、元の場所には一行のポインタを残す。タスクが完了したらテーブル行をアーカイブへ移し、レンジ行（例：`T1–T115`）へ畳み込む——レンジ行があれば「全タスクがテーブル内で参照可能」という既存チェックが成立し続けます。
3. **台帳は百件単位でアーカイブ**（D001–D100 で一ファイル。番号を見ればどのファイルか分かる）。live には直近 30 件ほどだけを残し、**機械生成・決定的出力**のインデックスを添える（決定的＝同じ入力なら必ず同じ出力、タイムスタンプなし。ゆえに「再生成 == ディスク上の内容」が鮮度チェックを兼ねる）。
4. **サイズはゲートスクリプトが機械的に見張り、赤がローテーションの唯一のトリガー。** ログに書いた規律は機能しません——スクリプトに載せて初めて機能します。意図的に単一トリガー：「N 件に達したら」のような第二トリガーは設けない。二つのルールが喧嘩する余地をなくすためです。ゲート導入時は**陽性＋陰性の校正**を行い（わざと違反を注入して検出できることを、正常フローで誤報しないことを確認）、有効化当日に本物の陽性対照を一度実行する（バジェットを現在サイズ未満へ一時的に下げる → 赤 → 戻す → 緑）。
5. **移行とローテーションの三鉄則**：
   - **移動のみ・改変なし**：純粋な移動で、機械的に再検証できること（sha256 / multiset アサーション。MIGRATION.ja.md のスニペット参照）；
   - **番号は不変**：決定番号・タスク番号は永久に変えない。リポジトリ中の相互参照がすべて生き残る；
   - **スクリプトによる保存則検証**：移動後、全番号の存在と総数の不変を機械的に検証する——**アサーションが失敗したら一バイトも書き込まない**。
   さらに包括原則をひとつ：**アーカイブ ≠ 削除**。アーカイブも追記専用。アーカイブ内の結論を覆すときは、live 側に「何を置き換えるか」を明記した新エントリを追加し、原文には触れない。

## 4. 新規プロジェクトの初日

- `docs/` ＋ `docs/archive/` を作る。入口ドキュメントに固定の読み順を書き切り、履歴は常にアーカイブへ、必要時に grep。
- 意思決定台帳は初日から「一決定一行・連番」で始める。追記専用と置換（supersession）のルールを明文化する。
- ゲートスクリプトは最初の一週間以内に導入（バジェットは緩めに始めて後で締めてよい）。バジェット＝実測サイズ × 1.5–2.5。
- 既存ツールで始める手も大いにあり（新規プロジェクトの移行コストゼロは、それらの最良のユースケース）：Beads（git 内蔵のエージェント用タスク DB）、Backlog.md（一タスク一 markdown）、OpenSpec（Propose → Apply → Archive のライフサイクル）。**走行中の乗り換えは別問題**——本プレイブックの出どころのプロジェクトは検討の末、ツールは替えずルールだけを導入する道を選びました。

## 5. すでに肥大化したプロジェクトの移行

`MIGRATION.ja.md` を参照（五段階の手順＋アサーション・スニペット）。最重要の三文だけ先に：

- 移行スクリプトは**内容でアンカーし、行番号を使わない**——複数セッションが並走していると、行番号は誰かにいつでもずらされます（出どころのプロジェクトは移行当日にまさにこれを食らいました）。
- **これらのドキュメントを読むツールは、同じコミットの中でコーパスを拡張する**——出どころのプロジェクトには、タスクテーブルをコーパスとする「先行事例チェック」ツールがあり、アーカイブ行を取り込まなければ、そのツール自身の看板事例が偽陰性になるところでした。
- 各ステップの間で：全ゲート緑 → コミット → プッシュ。

## 6. 赤字の書き方

すべての赤には**処方箋を添える**（「超過 → この具体的アクションをせよ」）。「超過しました」だけの報告は不可。オオカミ少年ゲートはゲートなしより悪い。同様に、**黙って緑を返しうる物差しは物差しなしより悪い**——校正には必ず「一度も変更していないものを測らせて、物差しが本当に測れることを確かめる」対照を一本含めます。

ツールのコメントに焼き込んだ、実戦の教訓二つ：

- BSD の `awk length` は**バイト**を数え、Python の `len` は**文字**を数える——数字を報告する前に単位を言うこと；
- 「全チェックがスキップ」は「合格」ではない：未設定で全部スキップされたときは「有効なチェックがひとつもない」と大声で言うこと。さもないと exit 0 が「統治済み」と読まれてしまう。
