# 决策账本（append-only）

> 记录「为什么这样定」。改变过去的决策＝新增一条并注明取代关系，**不删旧条目**。
> 规矩（见 PLAYBOOK §三-3）：一条一行、编号从 D1 连续递增；live 只留最近约 30 条，
> 更老的按百分段归档到 `docs/archive/DECISIONS-D001-D100.md` 等；轮转的唯一触发＝
> `tools/check_doc_size.py` 报红。快查索引：`docs/DECISIONS-INDEX.md`（derived，非权威）。

| # | 日期 | 决策 | 理由/背景 |
|---|------|------|-----------|
| D1 | 01-01 | （示例，替换我）采用本文档治理模板；预算与行长上限按 check_doc_size.py CONFIG | 权威文档无归档规则必然无限变长 |
