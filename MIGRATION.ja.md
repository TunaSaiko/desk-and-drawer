[English](MIGRATION.md) ｜ [简体中文](MIGRATION.zh.md) ｜ **日本語**

# 移行マニュアル：すでに肥大化したプロジェクトのために

新規プロジェクトは `templates/` をそのまま使ってください（README.ja.md のクイックスタート参照）。本マニュアルは、**「正」のドキュメントがすでに制御不能**になったプロジェクト向けです：一文字も失わずに履歴をアーカイブへ移す方法。出どころの事例：開始 7 週の非公開プロジェクトで、ボード 663KB・台帳 819KB・最太の一行 76,234 バイト。移行後、live はそれぞれ 237KB / 112KB、履歴の損失ゼロ。

## 三つの鉄則（手を動かす前に暗記）

1. **移動のみ・改変なし**：純粋な移動で、内容は一文字も変えない。sha256 / multiset アサーションで証明する（コードは下記）。
2. **番号は不変**：決定番号・タスク番号は永久に変えない。リポジトリ中の「D141 参照」式の相互参照が、アーカイブ後もすべて解決できる。
3. **スクリプトによる保存則検証**：**全アサーションが通ってから**書き込む——一つでも失敗したら一バイトも書かない。（我々の v1 スクリプトは自分自身のアサーションに止められました。止められて正解でした。）

## 五段階の手順（各ステップの間で：全ゲート緑 → コミット → プッシュ）

1. **決定を記録し、最悪の一行モンスターを解体する。** 誰が承認し、なぜ、コストは何か——まず台帳へ。その後、超長行を丸ごとアーカイブへ移し（改行を挿入するだけ、一文字も変えない）、live 側は一行のスナップショットに置き換えて「行ごと置換」の新ルールをその場に明記する。
2. **ボードの履歴三ブロックを引っ越す**：ステージログ／完了タスク行／更新ログ → アーカイブ三ファイルへ。元の場所にはポインタを残す。タスクテーブルには**レンジ行**（例：`| T1–T115 | … | closed | — |`）を置き、「全タスクがテーブル内で参照可能」を保つ。
3. **台帳を百件単位でアーカイブ**（D001–D100 で一ファイル）。live には直近 30 件ほどを残し、インデックスを生成。
4. **ゲートを稼働させる**（`tools/check_doc_size.py` の CONFIG を書き換え）＋ ルールをルール文書へ、「ゲートを走らせる」をエージェント入口ドキュメントへ書き込む。**ゲートの名簿と読み順は同じコミットで更新すること**——「ゲートは増えたのに名簿が追いつかない」は再発率の高いドリフトです。
5. **最終確認**：全ゲート緑、`git status` クリーン、プッシュ済み。移行スクリプトとそのアサーション出力は証拠としてリポジトリに残す。

## 実戦の教訓四つ（すべて実際に踏んだもの）

- **内容でアンカーし、行番号を使わない。** 節は見出しで特定する（`find(lines, "## 二、")`）。並走セッションがあると行番号はいつでもずらされます（出どころのプロジェクトでは、移行当日の最中に別セッションのコミットが割り込みました）。
- **これらのドキュメントを読むツールは、同じコミットでコーパスを拡張する。** 出どころのプロジェクトには、タスクテーブルをコーパスとする「先行事例チェック」ツールがあり、アーカイブファイルを取り込まなければ、そのツール自身の看板事例が偽陰性になるところでした。引っ越し前に `grep -l` で、これらのドキュメントを読むスクリプトを全部洗い出すこと。
- **数字を報告する前に単位を言う。** BSD の `awk length` はバイト、Python の `len` は文字を数え、CJK テキストでは 2 倍以上違います。出どころのプロジェクトは「76,234 文字」（実際はバイト）を文書に残しかけ、未来の監査者に偽のドリフトを製造するところでした。
- **移行後に本物の陽性対照を一度。** ゲート稼働当日：`--probe` でバジェットを現在サイズ未満へ → 赤を確認 → 戻す → 緑を確認。この三手順を作業ログに書く——壊れているのに緑を返す物差しは、物差しなしより悪い。

## アサーション・スニペット（移行スクリプトへコピーして使う）

```python
from collections import Counter
import hashlib, re

# ① 移動のみ・改変なし（行単位の引っ越し）：元ファイルの行 ==
#    新 live の行 ⊎ 各アーカイブへ移した行 − 明示登録した追加行
new_c = Counter(new_lines) + Counter(moved_a) + Counter(moved_b)
new_c.subtract(added_to_live)          # ポインタ／レンジ行／スタブを一件ずつ明示登録
diff  = {k: v for k, v in (new_c - Counter(orig_lines)).items() if k.strip()}
diff2 = {k: v for k, v in (Counter(orig_lines) - new_c).items() if k.strip()}
assert not diff and not diff2, ("余剰", list(diff)[:3], "喪失", list(diff2)[:3])

# ② 移動のみ・改変なし（長行の分割）：挿入した改行を除去すると
#    原文の行と完全一致しなければならない
assert split_text.replace("\n", "") == original_line
assert hashlib.sha256(split_text.replace("\n", "").encode()).hexdigest() == orig_sha

# ③ 番号の保存則：live＋アーカイブ全体で 1..max、各番号ちょうど一件
nums = sorted(all_ids)
assert nums == list(range(1, max(nums) + 1)), "番号の欠落または重複"

# ④ タスク行の保存則＋参照可能性（レンジ行を T(\d+)[–-]T?(\d+) で
#    展開して 1..N を覆うこと）
assert len(orig_rows) == len(archived_rows) + len(kept_rows)
```

## 移行スクリプトの形（参考）

出どころのプロジェクトの一回限りスクリプト二本（各 150 行前後）はこの骨格です：冪等性アサーション（移行済みを検知したら実行拒否）→ 内容アンカーで節を抽出 → 新 live とアーカイブ本体を組み立て → **全アサーション** → アーカイブを書いて読み戻し検証 → live を書く → 保存則の数字を出力（ブロック数／行数／バイト数はコミットメッセージへ）。上のスニペットを組み合わせれば作れます。
